#include <stdio.h>

int main(void) {
  int *p;
  int a=10;
  printf("o valor de A é: %d \n",a);
  printf("o endereço de A é: %p \n",&a);
  //algumas saídas usam o %x para a saída em decimal //

  return 0;
}